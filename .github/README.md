# .github

GitHub-platform configuration for `fiducia-test-config`.

- `dependabot.yml` — weekly dependency-update PRs for npm packages and GitHub Actions.
- `workflows/` — CI that runs the harness self-tests.
