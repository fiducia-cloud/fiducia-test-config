{
  "name": "@fiducia/test-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared browser-test harness + config presets for fiducia.cloud. Exports config/helpers only — each repo keeps its own specs. Playwright and Puppeteer are used as libraries under node --test.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:secrets": "node scripts/check-secret-policy.mjs",
    "lint:ores": "if [ -f .ores-lint/lint.sh ]; then sh .ores-lint/lint.sh; else echo 'ores-lint not installed'; fi",
    "prepublishOnly": "npm run lint:ores"
  },
  "exports": {
    "./harness": "./src/harness.mjs",
    "./assert": "./src/assert.mjs",
    "./stubs": "./src/stubs.mjs",
    "./shared-auth-stub": "./src/shared-auth-stub.mjs",
    "./secret-policy": "./src/secret-policy.mjs",
    "./tsconfig": "./tsconfig.base.json",
    "./eslint": "./eslint.config.base.mjs"
  },
  "files": [
    "src",
    "tsconfig.base.json",
    "eslint.config.base.mjs"
  ],
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
